{
  "name": "hostel-root",
  "private": true,
  "version": "1.0.0",
  "description": "Root package to delegate to Hostel app",
  "scripts": {
    "start": "npm --prefix Hostel start",
    "dev": "npm --prefix Hostel run dev",
    "build": "npm --prefix Hostel run build",
    "test": "npm --prefix Hostel test",
    "postinstall": "npm --prefix Hostel install"
  }
}
